#include <stdio.h>

int main()
{
    int a[5] , b;

    for(b = 0; b <= 4; b++)

    {
        scanf("%d", &a[b]);
    }

        for(b = 0; b <= 4; b++)

        {
            printf("%d\n", a[b]);
        }

    return 0;
}
